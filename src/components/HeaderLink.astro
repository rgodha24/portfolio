---
export interface Props {
   href: string;
   target?: "_blank" | "_self" | "_parent" | "_top";
}

const { href, target } = Astro.props;
---

<a href={href} class="mt-auto text-sky-600 font-bold hover:text-sky-500" target={target || ""}>
   <slot />
</a>
